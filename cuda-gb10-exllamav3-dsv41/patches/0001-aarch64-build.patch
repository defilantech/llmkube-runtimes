diff --git a/exllamav3/exllamav3_ext/avx2_target.cpp b/exllamav3/exllamav3_ext/avx2_target.cpp
index b64c047..52b3446 100644
--- a/exllamav3/exllamav3_ext/avx2_target.cpp
+++ b/exllamav3/exllamav3_ext/avx2_target.cpp
@@ -5,7 +5,9 @@ bool is_avx2_supported()
     static bool avx2_check = false;
     static bool avx2_supported = false;
     if (avx2_check) return avx2_supported;
-    #ifdef __linux__
+    #if defined(__aarch64__)
+        (void)0; /* no cpuid here, report unsupported */
+    #elif defined(__linux__)
         avx2_supported = __builtin_cpu_supports("avx2");
     #else
         int cpuInfo[4];
@@ -22,7 +24,9 @@ bool is_f16c_supported()
     static bool f16c_check = false;
     static bool f16c_supported = false;
     if (f16c_check) return f16c_supported;
-    #ifdef __linux__
+    #if defined(__aarch64__)
+        (void)0; /* no cpuid here, report unsupported */
+    #elif defined(__linux__)
         f16c_supported = __builtin_cpu_supports("f16c");
     #else
         int cpuInfo[4];
diff --git a/exllamav3/exllamav3_ext/avx2_target.h b/exllamav3/exllamav3_ext/avx2_target.h
index f5d75f6..88db109 100644
--- a/exllamav3/exllamav3_ext/avx2_target.h
+++ b/exllamav3/exllamav3_ext/avx2_target.h
@@ -7,7 +7,7 @@
 bool is_avx2_supported();
 bool is_f16c_supported();
 
-#ifdef __linux__
+#if defined(__linux__) && (defined(__x86_64__) || defined(__i386__))
     #define AVX2_TARGET __attribute__((target("avx2")))
     #define AVX2_F16C_TARGET __attribute__((target("avx2,f16c")))
     #define AVX2_TARGET_OPTIONAL __attribute__((target_clones("avx2","default")))
diff --git a/exllamav3/exllamav3_ext/avx512_target.cpp b/exllamav3/exllamav3_ext/avx512_target.cpp
index 2a676f6..3566c60 100644
--- a/exllamav3/exllamav3_ext/avx512_target.cpp
+++ b/exllamav3/exllamav3_ext/avx512_target.cpp
@@ -6,7 +6,9 @@ bool is_avx512_supported()
     static bool avx512_supported = false;
     if (avx512_check) return avx512_supported;
 
-#ifdef __linux__
+#if defined(__aarch64__)
+        (void)0; /* no cpuid here, report unsupported */
+    #elif defined(__linux__)
     // Check for AVX-512F and AVX-512BW support
     avx512_supported = __builtin_cpu_supports("avx512f") && __builtin_cpu_supports("avx512bw");
 #else
diff --git a/exllamav3/exllamav3_ext/avx512_target.h b/exllamav3/exllamav3_ext/avx512_target.h
index d953709..fa06400 100644
--- a/exllamav3/exllamav3_ext/avx512_target.h
+++ b/exllamav3/exllamav3_ext/avx512_target.h
@@ -6,7 +6,7 @@
 
 bool is_avx512_supported();
 
-#ifdef __linux__
+#if defined(__linux__) && (defined(__x86_64__) || defined(__i386__))
     #define AVX512_TARGET __attribute__((target("avx512f,avx512bw")))
     #define AVX512_TARGET_OPTIONAL __attribute__((target_clones("avx512f,avx512bw","default")))
 #else
diff --git a/exllamav3/exllamav3_ext/cpu/moe_handoff.cu b/exllamav3/exllamav3_ext/cpu/moe_handoff.cu
index 2550b1d..ac0aea4 100644
--- a/exllamav3/exllamav3_ext/cpu/moe_handoff.cu
+++ b/exllamav3/exllamav3_ext/cpu/moe_handoff.cu
@@ -149,7 +149,9 @@ namespace {
 
 inline void cpu_pause_()
 {
-#ifdef __linux__
+#if defined(__aarch64__)
+    __asm__ __volatile__("yield");
+#elif defined(__linux__)
     __builtin_ia32_pause();
 #else
     _mm_pause();
diff --git a/exllamav3/exllamav3_ext/cpu/moe_mul1.cpp b/exllamav3/exllamav3_ext/cpu/moe_mul1.cpp
index 8e7ca23..23d6b2d 100644
--- a/exllamav3/exllamav3_ext/cpu/moe_mul1.cpp
+++ b/exllamav3/exllamav3_ext/cpu/moe_mul1.cpp
@@ -1,4 +1,89 @@
 #include "moe_mul1.h"
+#if defined(__aarch64__)
+// aarch64: this file is the x86 CPU MoE expert GEMM (AVX-512 VNNI) and includes
+// immintrin.h, which does not exist on aarch64. cpu/moe_handoff.cu resolves
+// these symbols unconditionally, so they are stubbed rather than dropped, and
+// the CPU MoE offload reports unavailable. The CPU MoE offload path is not used
+// by a single-B10 TP1 GPU run, which is what this image serves; the GPU and ATS
+// paths are unaffected.
+#include <cstdio>
+
+namespace
+{
+    void cpu_moe_unavailable_()
+    {
+        static bool warned = false;
+        if (!warned)
+        {
+            warned = true;
+            std::fprintf(stderr,
+                         "[exllamav3] CPU MoE offload is unavailable on aarch64 "
+                         "(x86-only AVX-512 kernel); request ignored.\n");
+        }
+    }
+}
+
+int64_t exl3_moe_cpu_make_layer
+(
+    const std::vector<at::Tensor>&,
+    const std::vector<at::Tensor>&,
+    const std::vector<at::Tensor>&,
+    const std::vector<at::Tensor>&,
+    const std::vector<at::Tensor>&,
+    const std::vector<at::Tensor>&,
+    const std::vector<at::Tensor>&,
+    const std::vector<at::Tensor>&,
+    const std::vector<at::Tensor>&,
+    const std::vector<at::Tensor>&,
+    const std::vector<at::Tensor>&,
+    const std::vector<at::Tensor>&,
+    int64_t,
+    double,
+    int64_t
+)
+{
+    cpu_moe_unavailable_();
+    return -1;
+}
+
+void exl3_moe_cpu_free_layer(int64_t) {}
+void exl3_moe_cpu_stage_experts(int64_t, const uint32_t*, int, uint8_t*, int) {}
+void exl3_moe_cpu_set_prof(bool) {}
+int64_t exl3_moe_cpu_pool_stress(int, int, int, int) { return 0; }
+bool exl3_moe_cpu_has_avx2() { return false; }
+bool exl3_moe_cpu_has_avx512_bw() { return false; }
+bool exl3_moe_cpu_has_avx512_vnni() { return false; }
+bool exl3_moe_cpu_has_avx512_vbmi() { return false; }
+
+void exl3_moe_cpu_forward
+(
+    int64_t,
+    const at::Tensor&,
+    const at::Tensor&,
+    const at::Tensor&,
+    at::Tensor&,
+    int64_t
+)
+{
+    cpu_moe_unavailable_();
+}
+
+void exl3_moe_cpu_forward_raw
+(
+    int64_t,
+    const at::Half*,
+    const int32_t*,
+    const at::Half*,
+    float*,
+    int,
+    int,
+    int
+)
+{
+    cpu_moe_unavailable_();
+}
+
+#else
 #include <c10/util/Half.h>
 #include <torch/extension.h>
 
@@ -2512,3 +2597,5 @@ void exl3_moe_cpu_forward
         static_cast<int>(num_threads)
     );
 }
+
+#endif  // !__aarch64__
diff --git a/exllamav3/exllamav3_ext/parallel/all_reduce_cpu.cu b/exllamav3/exllamav3_ext/parallel/all_reduce_cpu.cu
index 5b3688f..300abc3 100644
--- a/exllamav3/exllamav3_ext/parallel/all_reduce_cpu.cu
+++ b/exllamav3/exllamav3_ext/parallel/all_reduce_cpu.cu
@@ -109,7 +109,9 @@ void run_cpu_reduce_jobs
                 break;
             if (++spins < 65536)
             {
-                #ifdef __linux__
+                #if defined(__aarch64__)
+                    __asm__ __volatile__("yield");
+                #elif defined(__linux__)
                     __builtin_ia32_pause();
                 #else
                     _mm_pause();
diff --git a/exllamav3/exllamav3_ext/parallel/all_reduce_cpu_avx2.cpp b/exllamav3/exllamav3_ext/parallel/all_reduce_cpu_avx2.cpp
index a60daa2..0a14d16 100644
--- a/exllamav3/exllamav3_ext/parallel/all_reduce_cpu_avx2.cpp
+++ b/exllamav3/exllamav3_ext/parallel/all_reduce_cpu_avx2.cpp
@@ -1,3 +1,45 @@
+#if defined(__aarch64__)
+// aarch64: this file is the AVX2 CPU all-reduce (immintrin.h, _mm_pause). The
+// exported symbols are stubbed because parallel/all_reduce_cpu.cu (portable)
+// references perform_cpu_reduce() and enable_fast_fp() unconditionally. The
+// flush-to-zero setters become no-ops; the reduce entry points throw rather than
+// return wrong data, so misuse is loud. This path is not exercised by a
+// single-GB10 TP1 GPU run.
+#include "all_reduce_cpu_avx2.h"
+#include <stdexcept>
+
+void enable_fast_fp() {}
+void enable_fast_fp_avx2() {}
+
+void perform_cpu_reduce
+(
+    PGContext*,
+    size_t,
+    uint32_t,
+    uint32_t,
+    uint8_t*,
+    size_t
+)
+{
+    throw std::runtime_error(
+        "exllamav3: CPU all-reduce is unavailable on aarch64 (x86-only AVX2 kernel)");
+}
+
+void perform_cpu_reduce_avx2
+(
+    PGContext*,
+    size_t,
+    uint32_t,
+    uint32_t,
+    uint8_t*,
+    size_t
+)
+{
+    throw std::runtime_error(
+        "exllamav3: CPU all-reduce AVX2 is unavailable on aarch64");
+}
+
+#else
 #include <immintrin.h>
 #include "all_reduce_cpu_avx2.h"
 #include "all_reduce_cpu_avx512.h"
@@ -397,3 +439,5 @@ void perform_cpu_reduce_avx2
         chunk_idx++;
     }
 }
+
+#endif  // !__aarch64__
diff --git a/exllamav3/exllamav3_ext/parallel/all_reduce_cpu_avx512.cpp b/exllamav3/exllamav3_ext/parallel/all_reduce_cpu_avx512.cpp
index 7894ec4..3fdf053 100644
--- a/exllamav3/exllamav3_ext/parallel/all_reduce_cpu_avx512.cpp
+++ b/exllamav3/exllamav3_ext/parallel/all_reduce_cpu_avx512.cpp
@@ -1,3 +1,50 @@
+#if defined(__aarch64__)
+// aarch64: this file is the AVX-512 CPU all-reduce / bf16-fp16 add kernels
+// (immintrin.h). Stubbed for the same reason as all_reduce_cpu_avx2.cpp: the
+// symbols are resolved unconditionally, and the compute entry points throw
+// rather than return wrong data. Not exercised by a single-GB10 TP1 GPU run.
+#include "all_reduce_cpu_avx512.h"
+#include <cstddef>
+#include <cstdint>
+#include <stdexcept>
+
+void enable_fast_fp_avx512() {}
+
+void bf16_add_inplace_avx512(uint16_t*, const uint16_t*, size_t)
+{
+    throw std::runtime_error("exllamav3: AVX-512 bf16 add is unavailable on aarch64");
+}
+
+void bf16_add_twosrc_avx512(uint16_t*, const uint16_t*, const uint16_t*, size_t)
+{
+    throw std::runtime_error("exllamav3: AVX-512 bf16 add is unavailable on aarch64");
+}
+
+void fp16_add_inplace_avx512(uint16_t*, const uint16_t*, size_t)
+{
+    throw std::runtime_error("exllamav3: AVX-512 fp16 add is unavailable on aarch64");
+}
+
+void fp16_add_twosrc_avx512(uint16_t*, const uint16_t*, const uint16_t*, size_t)
+{
+    throw std::runtime_error("exllamav3: AVX-512 fp16 add is unavailable on aarch64");
+}
+
+void perform_cpu_reduce_avx512
+(
+    PGContext*,
+    size_t,
+    uint32_t,
+    uint32_t,
+    uint8_t*,
+    size_t
+)
+{
+    throw std::runtime_error(
+        "exllamav3: CPU all-reduce AVX-512 is unavailable on aarch64");
+}
+
+#else
 #include <immintrin.h>
 #include "all_reduce_cpu_avx512.h"
 #include "all_reduce_cpu_avx2.h"
@@ -418,3 +465,5 @@ void perform_cpu_reduce_avx512
         chunk_idx++;
     }
 }
+
+#endif  // !__aarch64__
